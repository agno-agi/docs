---
title: "Agent Version Management" 
sidebarTitle: "Version"
---

```python
# Each save creates a new version
agent.instructions = "Updated instructions"
agent.save()  # Creates version 2

# Load specific version (if supported)
# agent = get_agent_by_id(db=db, id="my-agent", version=1, registry=registry)
```