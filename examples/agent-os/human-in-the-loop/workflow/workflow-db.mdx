---
title: "Workflow DB"
description: "Shared Postgres database used by the workflow human-in-the-loop examples."
source: cookbook/05_agent_os/human_in_the_loop/workflow/workflow_db.py
---

```python workflow_db.py
from agno.db.postgres import PostgresDb

db_url = "postgresql+psycopg://ai:ai@localhost:5532/ai"
db = PostgresDb(db_url=db_url)
```

## Run the Example

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="Install dependencies">
    ```bash
    uv pip install -U agno "psycopg[binary]" sqlalchemy
    ```
  </Step>

  <Snippet file="run-pgvector-step.mdx" />

  <Step title="Use the helper">
    This shared database module is imported by the workflow examples in the same directory.
  </Step>
</Steps>

Full source: [cookbook/05_agent_os/human_in_the_loop/workflow/workflow_db.py](https://github.com/agno-agi/agno/blob/v2.7.4/cookbook/05_agent_os/human_in_the_loop/workflow/workflow_db.py)
