---
title: Vector Databases
sidebarTitle: Vector Databases
description: 65 examples covering 25+ vector databases for knowledge storage and retrieval.
---

Agno supports 25+ vector databases with a unified interface. Switch databases by changing one line of code.

```python
from agno.knowledge.knowledge import Knowledge
from agno.vectordb.pgvector import PgVector

knowledge = Knowledge(
    vector_db=PgVector(table_name="docs", db_url="postgresql://..."),
)
```

## Supported Databases

| Database | Import | Best For |
|----------|--------|----------|
| PgVector | `from agno.vectordb.pgvector import PgVector` | PostgreSQL users, production |
| Pinecone | `from agno.vectordb.pinecone import Pinecone` | Managed, serverless |
| Qdrant | `from agno.vectordb.qdrant import Qdrant` | High performance, filtering |
| Weaviate | `from agno.vectordb.weaviate import Weaviate` | Hybrid search, GraphQL |
| Milvus | `from agno.vectordb.milvus import Milvus` | Scale, distributed |
| ChromaDB | `from agno.vectordb.chroma import Chroma` | Local development, prototyping |
| LanceDB | `from agno.vectordb.lancedb import LanceDb` | Embedded, serverless |
| MongoDB | `from agno.vectordb.mongodb import MongoDB` | Document store users |
| Redis | `from agno.vectordb.redis import Redis` | Low latency, caching |
| Cassandra | `from agno.vectordb.cassandra import Cassandra` | Scale, distributed |
| ClickHouse | `from agno.vectordb.clickhouse import ClickHouse` | Analytics, OLAP |
| SingleStore | `from agno.vectordb.singlestore import SingleStore` | Real-time analytics |
| Upstash | `from agno.vectordb.upstash import Upstash` | Serverless, edge |
| Couchbase | `from agno.vectordb.couchbase import Couchbase` | Mobile, edge sync |
| SurrealDB | `from agno.vectordb.surrealdb import SurrealDB` | Multi-model |

## Examples by Database

### PgVector

PostgreSQL extension for vector similarity search.

```python cookbook/07_knowledge/vector_db/pgvector/pgvector_db.py
from agno.agent import Agent
from agno.knowledge.knowledge import Knowledge
from agno.vectordb.pgvector import PgVector

knowledge = Knowledge(
    vector_db=PgVector(
        table_name="documents",
        db_url="postgresql+psycopg://ai:ai@localhost:5532/ai",
    ),
)

knowledge.add_content(url="https://example.com/docs.pdf")

agent = Agent(knowledge=knowledge, search_knowledge=True)
agent.print_response("Summarize the documentation")
```

### Hybrid Search with PgVector

Combine vector and keyword search.

```python cookbook/07_knowledge/vector_db/pgvector/pgvector_hybrid_search.py
from agno.vectordb.pgvector import PgVector

vector_db = PgVector(
    table_name="hybrid_docs",
    db_url="postgresql://...",
    search_type="hybrid",  # Enable hybrid search
)
```

### Pinecone

Fully managed vector database.

```python cookbook/07_knowledge/vector_db/pinecone_db/pinecone_db.py
from agno.knowledge.knowledge import Knowledge
from agno.vectordb.pinecone import Pinecone

knowledge = Knowledge(
    vector_db=Pinecone(
        name="my-index",
        api_key="your-api-key",
        environment="us-east-1",
    ),
)
```

### Qdrant

High-performance vector database with rich filtering.

```python cookbook/07_knowledge/vector_db/qdrant_db/qdrant_db.py
from agno.knowledge.knowledge import Knowledge
from agno.vectordb.qdrant import Qdrant

knowledge = Knowledge(
    vector_db=Qdrant(
        collection="documents",
        url="http://localhost:6333",
    ),
)
```

### Weaviate

Vector database with hybrid search and GraphQL.

```python cookbook/07_knowledge/vector_db/weaviate_db/weaviate_db.py
from agno.knowledge.knowledge import Knowledge
from agno.vectordb.weaviate import Weaviate

knowledge = Knowledge(
    vector_db=Weaviate(
        collection="Documents",
        url="http://localhost:8080",
    ),
)
```

### Milvus

Distributed vector database for scale.

```python cookbook/07_knowledge/vector_db/milvus_db/milvus_db.py
from agno.knowledge.knowledge import Knowledge
from agno.vectordb.milvus import Milvus

knowledge = Knowledge(
    vector_db=Milvus(
        collection="documents",
        uri="http://localhost:19530",
    ),
)
```

### ChromaDB

Embedded vector database for development.

```python cookbook/07_knowledge/vector_db/chroma_db/chroma_db.py
from agno.knowledge.knowledge import Knowledge
from agno.vectordb.chroma import Chroma

knowledge = Knowledge(
    vector_db=Chroma(
        collection="local_docs",
        path="./chroma_data",
    ),
)
```

### LanceDB

Serverless vector database with native Python.

```python cookbook/07_knowledge/vector_db/lance_db/lance_db.py
from agno.knowledge.knowledge import Knowledge
from agno.vectordb.lancedb import LanceDb

knowledge = Knowledge(
    vector_db=LanceDb(
        table_name="documents",
        uri="./lancedb",
    ),
)
```

### MongoDB Atlas

Vector search on MongoDB.

```python cookbook/07_knowledge/vector_db/mongo_db/mongo_db.py
from agno.knowledge.knowledge import Knowledge
from agno.vectordb.mongodb import MongoDB

knowledge = Knowledge(
    vector_db=MongoDB(
        collection="documents",
        uri="mongodb+srv://...",
        database="knowledge",
    ),
)
```

### Redis

In-memory vector search.

```python cookbook/07_knowledge/vector_db/redis_db/redis_db.py
from agno.knowledge.knowledge import Knowledge
from agno.vectordb.redis import Redis

knowledge = Knowledge(
    vector_db=Redis(
        index_name="documents",
        url="redis://localhost:6379",
    ),
)
```

## All 65 Vector Database Examples

| Database | Examples |
|----------|----------|
| PgVector | `pgvector_db.py`, `pgvector_hybrid_search.py`, `async_pg_vector.py` |
| Pinecone | `pinecone_db.py`, `async_pinecone_db.py` |
| Qdrant | `qdrant_db.py`, `qdrant_db_hybrid_search.py`, `async_qdrant_db.py` |
| Weaviate | `weaviate_db.py`, `weaviate_db_hybrid_search.py`, `async_weaviate_db.py` |
| Milvus | `milvus_db.py`, `milvus_db_hybrid_search.py`, `async_milvus_db.py` |
| ChromaDB | `chroma_db.py`, `async_chroma_db.py` |
| LanceDB | `lance_db.py`, `lance_db_hybrid_search.py`, `async_lance_db.py` |
| MongoDB | `mongo_db.py`, `mongo_db_hybrid_search.py`, `async_mongo_db.py` |
| Redis | `redis_db.py`, `async_redis_db.py` |
| Cassandra | `cassandra_db.py`, `async_cassandra_db.py` |
| ClickHouse | `clickhouse.py`, `async_clickhouse.py` |
| SingleStore | `singlestore_db.py`, `async_singlestore_db.py` |
| Upstash | `upstash_db.py`, `async_upstash_db.py` |
| SurrealDB | `surreal_db.py`, `async_surreal_db.py` |
| Couchbase | `couchbase_db.py`, `async_couchbase_db.py` |
| LangChain | `langchain_db.py` - Use LangChain vector stores |
| LlamaIndex | `llamaindex_db.py` - Use LlamaIndex stores |
| LightRAG | `lightrag.py` - Graph-based RAG |

## Run Examples

```bash
git clone https://github.com/agno-agi/agno.git
cd agno/cookbook/07_knowledge/vector_db

# PgVector
python pgvector/pgvector_db.py

# Qdrant
python qdrant_db/qdrant_db.py

# ChromaDB (no external deps)
python chroma_db/chroma_db.py
```

## Next

- [Embedders](/examples/knowledge/embedders) - 29 embedding options
- [Document Readers](/examples/knowledge/readers) - Parse any document
- [Knowledge Overview](/examples/knowledge/overview) - All knowledge examples
