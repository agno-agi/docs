---
title: Workflow Session Naming
sidebarTitle: Overview
description: Organize and identify your workflow sessions with meaningful names
---

## What is Session Naming?

Session naming lets you give human-readable names to your workflow sessions instead of just using the auto-generated session IDs. This makes it easier to identify sessions in logs, databases, and user interfaces.

Unlike agent/team sessions which generate names from conversation content, workflow sessions use timestamp-based naming since they represent executions rather than conversations.

## How Workflow Session Naming Differs

| Feature | Agent/Team Sessions | Workflow Sessions |
|---------|-------------------|------------------|
| **Auto-generation** | Uses first 6 messages (max 5 words) | Uses timestamp + description |
| **Basis** | Content-based | Time-based |
| **When to generate** | After 2-3 conversation turns | Anytime after session creation |
| **Example** | "Python Best Practices" | "Research Pipeline - 2024-11-19 14:30" |

## Quick Examples

### Manual Naming

```python
from agno.workflow import Workflow
from agno.db.sqlite import SqliteDb

workflow = Workflow(
    name="Research Pipeline",
    db=SqliteDb(db_file="workflows.db"),
    steps=[...],
)

workflow.run(input="Analyze AI trends")
workflow.set_session_name(session_name="AI Trends Analysis Q4 2024")
```

### Auto-Generated Names

```python
workflow = Workflow(
    name="Research Pipeline",
    description="Automated research and analysis pipeline",
    db=SqliteDb(db_file="workflows.db"),
    steps=[...],
)

workflow.run(input="Research topic")
workflow.set_session_name(autogenerate=True)
# Result: "Automated research and analysis pipel - 2024-11-19 14:30"
```

## Name Format

Auto-generated names follow this format:

**Without description:**
```
"Workflow Session - YYYY-MM-DD HH:MM"
```

**With description:**
```
"{description} - YYYY-MM-DD HH:MM"
```

Description is truncated to 40 characters if longer.

## Usage Guides

<CardGroup cols={2}>
  <Card title="Manual Naming" icon="pencil" href="/basics/sessions/workflow-sessions/workflow-naming/usage/manual-naming">
    Set custom names for your sessions
  </Card>
  <Card title="Auto-Generation" icon="wand-magic-sparkles" href="/basics/sessions/workflow-sessions/workflow-naming/usage/auto-generation">
    Let Agno generate timestamp-based names
  </Card>
</CardGroup>

## Best Practices

### Be Descriptive

```python
# Good: Clear purpose
workflow.set_session_name(session_name="Customer Support - Ticket #12345")

# Avoid: Too vague
workflow.set_session_name(session_name="Session 1")
```

### Include Context

```python
# Good: Includes relevant context
workflow.set_session_name(session_name="Q4 2024 Sales Analysis - EMEA Region")

# Avoid: Missing important details
workflow.set_session_name(session_name="Sales")
```

### Use Consistent Naming Schemes

```python
# Good: Consistent format
"Project Alpha - Research Phase"
"Project Alpha - Implementation Phase"
"Project Beta - Research Phase"
```

## Related Topics

<CardGroup cols={2}>
  <Card title="Workflow History" icon="clock-rotate-left" href="/basics/sessions/workflow-sessions/workflow-history/overview">
    Access previous run results
  </Card>
  <Card title="Session State" icon="database" href="/basics/sessions/workflow-sessions/session-state/overview">
    Share data between steps
  </Card>
  <Card title="Overview" icon="book" href="/basics/sessions/workflow-sessions/overview">
    Back to workflow sessions basics
  </Card>
  <Card title="Metrics" icon="chart-line" href="/basics/sessions/workflow-sessions/metrics/overview">
    Track workflow performance
  </Card>
</CardGroup>

## Developer Resources

- See the [Workflow API](/reference/workflows/workflow)
- View the [WorkflowSession schema](/reference/workflows/session)

