---
title: Image Input URL
description: Point a Vertex AI Claude agent at an image URL and search the web for context.
---

Pass an image URL with `Image(url=...)`.

## Code

```python image_input_url.py
from agno.agent import Agent
from agno.media import Image
from agno.models.vertexai.claude import Claude
from agno.tools.websearch import WebSearchTools

agent = Agent(
    model=Claude(id="claude-sonnet-4@20250514"),
    tools=[WebSearchTools()],
    markdown=True,
)

agent.print_response(
    "Tell me about this image and search the web for more information.",
    images=[
        Image(
            url="https://fastly.picsum.photos/id/237/200/300.jpg?hmac=TmmQSbShHz9CdQm0NkEjx1Dyh_Y984R9LpNrpvH2D_U"
        ),
    ],
    stream=True,
)
```

## Usage

<Steps>
  <Snippet file="create-venv-step.mdx" />

<Step title="Set your environment variables">
  <CodeGroup>
    ```bash Mac
    export CLOUD_ML_REGION=xxx
    export GOOGLE_CLOUD_PROJECT=xxx
    ```
    ```bash Windows
    setx CLOUD_ML_REGION xxx
    setx GOOGLE_CLOUD_PROJECT xxx
    ```
  </CodeGroup>

</Step>

<Step title="Authenticate your CLI session">
  ```bash
  gcloud auth application-default login
  ```

<Note>You don't need to authenticate your CLI every time.</Note>
</Step>

  <Step title="Install dependencies">
    ```bash
    uv pip install -U 'anthropic[vertex]' ddgs agno
    ```
  </Step>

  <Step title="Run Agent">
    Save the code above as `image_input_url.py`, then run:
    ```bash
    python image_input_url.py
    ```
  </Step>
</Steps>
