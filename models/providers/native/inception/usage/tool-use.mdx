---
title: Agent with Tools
description: Give an Inception agent a web search tool and stream the response.
---

## Code

```python tool_use.py
from agno.agent import Agent
from agno.models.inception import Inception
from agno.tools.websearch import WebSearchTools

agent = Agent(
    model=Inception(id="mercury-2"),
    tools=[WebSearchTools()],
    markdown=True,
)

if __name__ == "__main__":
    agent.print_response("Whats happening in France?", stream=True)
```

## Usage

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="Set your API key">
    ```bash
    export INCEPTION_API_KEY=xxx
    ```
  </Step>

  <Step title="Install dependencies">
    ```bash
    uv pip install -U openai ddgs agno
    ```
  </Step>

  <Step title="Run Agent">
    Save the code above as `tool_use.py`, then run:
    ```bash
    python tool_use.py
    ```
  </Step>
</Steps>
