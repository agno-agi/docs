---
title: Async Agent with Tools
description: Give a Mistral agent web search and run it asynchronously.
---

## Code

```python async_tool_use.py
import asyncio

from agno.agent import Agent
from agno.models.mistral import MistralChat
from agno.tools.websearch import WebSearchTools

agent = Agent(
    model=MistralChat(id="mistral-large-latest"),
    tools=[WebSearchTools()],
    markdown=True,
)

asyncio.run(agent.aprint_response("What's happening in France?", stream=True))

```

## Usage

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="Set your API key">
    ```bash
    export MISTRAL_API_KEY=xxx
    ```
  </Step>

  <Step title="Install dependencies">
    ```bash
    uv pip install -U mistralai ddgs agno
    ```
  </Step>

  <Step title="Run Agent">
    ```bash
    python async_tool_use.py
    ```
  </Step>
</Steps>
