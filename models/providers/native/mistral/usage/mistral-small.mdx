---
title: Mistral Small
description: Run a Mistral Small agent with web search tools and streamed output.
---

## Code

```python mistral_small.py
from agno.agent import Agent
from agno.models.mistral import MistralChat
from agno.tools.websearch import WebSearchTools

agent = Agent(
    model=MistralChat(id="mistral-small-latest"),
    tools=[WebSearchTools()],
    markdown=True,
)
agent.print_response("Tell me about mistrall small, any news", stream=True)

```

## Usage

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="Set your API key">
    ```bash
    export MISTRAL_API_KEY=xxx
    ```
  </Step>

  <Step title="Install dependencies">
    ```bash
    uv pip install -U mistralai ddgs agno
    ```
  </Step>

  <Step title="Run Agent">
    Save the code above as `mistral_small.py`, then run:
    ```bash
    python mistral_small.py
    ```
  </Step>
</Steps>
