---
title: Image Transcribe Document Agent
description: Transcribe a scanned document image into text with a Ministral 14B agent.
---

## Code

```python image_transcribe_document_agent.py
from agno.agent import Agent
from agno.media import Image
from agno.models.mistral.mistral import MistralChat

agent = Agent(
    model=MistralChat(id="ministral-14b-2512"),
    markdown=True,
)

agent.print_response(
    "Transcribe this document.",
    images=[
        Image(url="https://ciir.cs.umass.edu/irdemo/hw-demo/page_example.jpg"),
    ],
)

```

## Usage

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="Set your API key">
    ```bash
    export MISTRAL_API_KEY=xxx
    ```
  </Step>

  <Step title="Install dependencies">
    ```bash
    uv pip install -U mistralai agno
    ```
  </Step>

  <Step title="Run Agent">
    Save the code above as `image_transcribe_document_agent.py`, then run:
    ```bash
    python image_transcribe_document_agent.py
    ```
  </Step>
</Steps>
