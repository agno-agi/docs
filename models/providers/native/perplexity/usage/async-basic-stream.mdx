---
title: Async Basic Streaming Agent
description: Stream a Perplexity-powered Agno agent's response asynchronously.
---

## Code

```python async_basic_stream.py
import asyncio

from agno.agent import Agent
from agno.models.perplexity import Perplexity

agent = Agent(model=Perplexity(id="sonar-pro"), markdown=True)

asyncio.run(agent.aprint_response("Share a 2 sentence horror story", stream=True))

```

## Usage

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="Set your API key">
    ```bash
    export PERPLEXITY_API_KEY=xxx
    ```
  </Step>

  <Step title="Install dependencies">
    ```bash
    uv pip install -U agno openai
    ```
  </Step>

  <Step title="Run Agent">
    ```bash
    python async_basic_stream.py
    ```
  </Step>
</Steps>
